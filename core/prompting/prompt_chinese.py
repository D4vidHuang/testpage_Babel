from .prompt_base import PromptBase


class PromptLibrary(PromptBase):
    """中文提示词模板，用于 LLM-as-Judge 评估。

    继承 PromptBase 中与语言无关的实用工具。
    此处仅定义中文特定的提示词内容。

    基于研究的增强路线图：
    ====================================
    此类需要根据最近的 LLM-as-a-Judge 研究进行更新。
    详见 references.bib 了解下文提到的论文完整引用。

    关键改进需求：
    1. 思维链 (CoT) 推理提示词 [@liu-etal-2023-g]
    2. 系统提示词中的偏见缓解 [@shi2025judgingjudgessystematicstudy]
    3. 基于量表的分类法格式化 [@kim2023prometheus, @Pathak_2025]
    4. 分层类别评估 [@lee2024improvingllmclassificationlogical]
    5. 多语言评估策略 [@xuan2025mmluproxmultilingualbenchmarkadvanced]
    """
    
    LANGUAGE_CODE = "chinese"
    
    @staticmethod
    def get_output_field_names() -> dict:
        """Chinese translations for JSON output field names."""
        return {
            "evaluations": "评估结果",
            "model_name": "模型名称",
            "errors": "错误",
            "error_id": "错误标识",
            "confidence": "置信度",
            "justification": "理由",
            "explanation": "解释",
            "reasoning": "推理",
            "overall_quality": "整体质量",
            "correct": "正确",
            "partially_correct": "部分正确",
            "incorrect": "不正确",
        }

    @staticmethod
    def get_rubric_labels() -> dict:
        """Chinese translations for rubric formatting labels."""
        return {
            "rubric_title": "错误分类标准",
            "definition": "定义",
            "mark_present": "标记为存在",
            "mark_absent": "标记为不存在",
        }

    @staticmethod
    def system_basic() -> str:
        """生成评估者角色的基础系统提示词。

        返回:
            描述评估者角色的系统提示词

        当前问题：极简提示词（“你是一位专家评估员...”）缺乏明确的
        偏见缓解指令。这可能导致：
        - 模型预测评估的不一致性
        - 偏向冗长但错误的注释
        - 多模型比较中的系统性偏见
        """
        return (
            "你是一位专家评估员。你根据给定的错误分类法分析 Java 代码、注释 "
            "和文档的正确性。\\n\\n"
            "偏见缓解指令：\\n"
            "- 避免冗长偏见：不要偏向较长的注释。根据正确性进行评估。\\n"
            "- 确保一致性：对所有预测应用相同的标准。\\n"
            "- 防止系统性偏见：根据分类法独立评估每个实例。"
        )

    @staticmethod
    def system_enhanced() -> str:
        """生成评估者角色的偏见缓解系统提示词。

        返回:
            描述评估者角色的详细系统提示词

        TODO [优先级 1]：使用偏见缓解增强系统提示词
        ==============================================================
        研究：[@shi2025judgingjudgessystematicstudy] 确定了 LLM-as-a-Judge 
        评估中的三个关键偏见：
        - 位置偏见：评判者倾向于列表中第一个/最后一个项目
        - 冗长偏见：评判者无论质量如何都给较长的回答打高分
        - 自我增强偏见：评判者倾向于自己模型的输出（GPT-4 对 GPT-4 输出
          显示约 10% 的偏见；Claude 对 Claude 输出显示约 25% 的偏见）

        另见：[@liu-etal-2023-g] 关于偏见缓解的 MT-Bench 基础研究。
        """
        return (
            "你是一位代码文档评估专家，具有软件工程和多语言文本分析的经验。"
            "你的任务是使用详细的分类法识别 LLM 生成的代码注释中的错误。\\n\\n"
            "评估原则：\n"
            "- 保持一致：无论注释的长度或冗长程度如何，都应用相同的标准\n"
            "- 精确判断：只有在明确满足包含标准时才标记错误\n"
            "- 检查包含/排除：在确认错误之前，始终验证包含/排除标准\n"
            "- 语言意识：考虑到注释可能使用非英语语言；适当评估目标语言的语法和语义\n"
            "- 上下文相关：使用周围的代码上下文来评估语义准确性\n"
            "- 位置无关：独立评估每个预测；不要让顺序影响你的判断\n\n"
            "重要提示：不要让注释的长度影响你对质量的判断。"
            "短注释可以是正确的；长注释可能包含错误。"
            "仅根据注释是否根据分类法标准准确描述代码来评估每条注释。"
        )

    @staticmethod
    def expert_accuracy_guidance() -> str:
        """生成专家准确度标签及其定义。"""
        return (
            "专家准确度标签：\n"
            "- 正确：预测注释涵盖了原始注释的所有信息；包含额外的相关细节也是被允许的。\n"
            "- 部分正确：注释大部分正确，但存在细微错误（如：轻微拼写错误、变量名对应错误或轻微遗漏）。\n"
            "- 不正确：注释遗漏了重要信息，或包含超出细微修复范畴的重大错误。"
        )

    @staticmethod
    def assignment_evaluate_models(taxonomy: str) -> str:
        """为评估分组模型预测生成任务提示词。

        参数:
            taxonomy: 错误分类法的格式化字符串表示

        返回:
            分组评估的任务分配提示词
        """
        return (
            "你被提供了一段 Java 代码，其中包含原始注释以及以 JSON 格式呈现的多个模型生成的预测结果。你的任务是评估每个模型预测的注释，并从中识别出分类法中定义的错误。\n\n"
            f"可能的注释错误分类法：\n{taxonomy}\n\n"
            "对于提供的 JSON 中的每一个模型预测：\n"
            "- 分析 predicted_comment 字段。\n"
            "- 判定该分类法中出现了哪些错误类型。\n"
            "- 在决策时，请参考包含和排除标准。\n"
            "- 如果不存在任何错误，请明确标注。\n\n"
            "评估原则：\n"
            "- 保持一致性：无论注释的长度或冗余程度如何，请始终应用相同的评估标准。\n"
            "- 追求精确：仅在明确符合包含标准时才标记错误。\n"
            "- 核查包含/排除标准：在确认错误之前，务必验证包含/排除标准。\n"
            "- 具备语言意识：考虑到注释可能采用非英语语言，请针对目标语言适当地评估其语法和语义。\n"
            "- 依赖上下文：利用周围的代码上下文来评估语义的准确性。\n"
            "- 位置无关：独立评估每个预测结果，避免位置顺序影响你的判断。\n\n"
            "输入数据以 JSON 格式提供，包含 source_code、original_comment 和 model_predictions 数组。"
        )

    @staticmethod
    def assignment_evaluate_cluster(cluster_name: str, cluster_taxonomy: str) -> str:
        """为特定的类别集群生成任务提示词。

        通过要求评判者仅评估错误类别的子集，创建聚焦的提示词，从而减轻认知负荷并提高准确性。

        参数:
            cluster_name: 错误集群的名称 (例如, "linguistic_grammar")
            cluster_taxonomy: 仅针对该集群的格式化分类法字符串

        返回:
            聚焦于特定错误集群的任务分配提示词
        """
        cluster_descriptions = {
            "linguistic_grammar": "语言与语法正确性",
            "linguistic_language": "语言一致性与恰当性",
            "semantic_accuracy": "语义准确性与完整性",
            "semantic_code": "代码片段准确性",
            "model_behavior": "模型特定错误与生成痕迹",
            "syntax_format": "注释格式与结构",
            "meta": "排除标准与杂项问题"
        }

        description = cluster_descriptions.get(cluster_name, cluster_name)

        return (
            f"你目前仅评估代码注释的 {description.upper()}。\\n\\n"
            f"此评估的相关错误类别：\\n{cluster_taxonomy}\\n\\n"
            "重要提示：请仅关注上述列出的错误类型。"
            "忽略其他潜在问题——它们将被单独评估。\\n\\n"
            "对于上述每个错误类别，请根据包含标准确定错误是否存在。"
            "在确认之前务必检查排除标准。"
        )

    @staticmethod
    def _structured_output_instruction_v2_cot() -> str:
        """已停用：请改用 output_with_reasoning()。
        
        此方法仅保留用于参考。
        
        生成结构化 JSON 输出指令。
        返回:
            JSON 格式规范的指令字符串

        [优先级 1]：添加思维链推理 (G-EVAL 风格)
        =================================================================
        研究：[@liu-etal-2023-g] G-Eval 通过在评分前要求逐步推理，
        实现了 0.514 的 Spearman 相关性（与人类评判），比基准 
        LLM 评估器提高了约 2 倍。

        当前问题：当前的提示词直接要求分类，而没有明确的推理步骤。这种
        “系统 1”思维会导致：
        - 与人工标注员的一致性较低
        - 在类似案例中检测错误的不一致性
        - 难以调试为何某些错误被标记或未被标记

        研究发现：要求模型在做出判断前阐明其推理过程（思维链）能显著提高：
        - 相似示例间的一致性 (+15-20%)
        - 与人类评判的对齐度 (+2 倍相关性)
        - 决策的可调试性和可解释性

        替代方案：带有示例的少样本提示词 (Few-shot prompting)
        ----------------------------------------------
        除了明确的 CoT 指令，提供 2-3 个显示推理过程的正确评估示例。
        参见 [@liu-etal-2023-g] 第 3.2 节关于少样本提示词工程策略。
        """
        return (
            "为每个错误类别遵循以下步骤：\\n"
            "1. 仔细阅读预测的注释\\n"
            "2. 根据填充中间 (FIM) 掩码技术，理解生成的注释所引用的代码部分\\n"
            "3. 将其与原始注释和代码上下文进行对比\\n"
            "4. 对于每个潜在错误，推理是否符合包含标准\\n"
            "5. 在确认错误前核查排除标准\\n"
            "6. 提供你的推理，然后给出分类\\n"
            "7. 基于上述推理，判定生成注释的整体质量为：正确、部分正确、不正确\\n\\n"
            "评估原则：\\n"
            "- 保持一致性：无论注释的长度或冗余程度如何，应用相同的标准\\n"
            "- 追求精确：仅在明确满足包含标准时标记错误\\n"
            "- 核查包含/排除：始终在确认错误前验证包含/排除标准\\n"
            "- 具备语言意识：考虑到注释可能使用非英语语言；适当地评估目标语言的语法和语义\\n"
            "- 上下文相关：使用周围的代码上下文来评估语义准确性\\n"
            "- 位置无关：独立评估每个预测；不要让顺序影响你的判断\\n\\n"
            f"{PromptLibrary.expert_accuracy_guidance()}\n\n"
            "返回具有以下格式的结构化 JSON：\\n"
            "{\\n"
            '  "推理": "步骤分析：首先，我观察到...",\\n'
            '  "错误": [\\n'
            "    {\\n"
            '      "错误标识": "<错误标识>",\\n'
            '      "置信度": <0.0-1.0>,\\n'
            '      "理由": "此错误适用，因为..."\\n'
            "    }\\n"
            "  ],\\n"
            '  "整体质量": "<正确|部分正确|不正确>"\n'
            "}\\n"
            "如果不包含任何错误，返回空的错误数组，并解释为什么注释是正确的。"
        )

    @staticmethod
    def output_basic() -> str:
        """为包含多个模型的分组数据评估生成指令。
        返回:
            包含模型特定结果的 JSON 格式指令字符串
        """
        return (
            f"{PromptLibrary.expert_accuracy_guidance()}\n\n"
            "返回每个模型结果的结构化 JSON：\n"
            "{\n"
            '  "评估结果": [\n'
            "    {\n"
            '      "模型名称": "<模型标识符>",\n'
            '      "错误": ["<错误标识>", ...],\n'
            '      "解释": "发现错误的简短说明。",\n'
            '      "整体质量": "<正确|部分正确|不正确>"\n'
            "    }\n"
            "  ]\n"
            "}\n"
            "如果模型没有错误，请使用空的错误数组。"
        )

    @staticmethod
    def output_with_reasoning() -> str:
        """为分组数据评估生成包含思维链 (CoT) 推理的指令。

        结合了多模型分组评估与思维链推理结构，以提高准确性和可解释性。

        研究基础：[@liu-etal-2023-g] G-Eval 表明，在分类前要求明确的推理步骤可将与人类评判的一致性提高 15-20%，并将相关性提高约 2 倍。

        返回:
            包含模型特定 CoT 结果的 JSON 格式指令字符串
        """
        return (
            "对于每个模型的预测注释，请遵循以下步骤：\n"
            "1. 仔细阅读预测出的注释\n"
            "2. 理解生成的注释所引用的代码部分（基于 FIM - 填充中间 - 掩码技术）\n"
            "3. 将其与原始注释及代码上下文进行对比\n"
            "4. 针对每一个潜在错误，推理其是否符合包含标准\n"
            "5. 在确认错误前，核查排除标准\n"
            "6. 提供你的推理过程，随后给出分类\n"
            "7. 基于上述推理，判定生成注释的整体质量为：正确、部分正确或不正确\n\n"
            "评估原则：\n"
            "- 保持一致性：无论注释的长度或冗余程度如何，请始终应用相同的评估标准\n"
            "- 追求精确：仅在明确符合包含标准时才标记错误\n"
            "- 核查包含/排除标准：在确认错误之前，务必验证包含/排除标准\n"
            "- 具备语言意识：考虑到注释可能采用非英语语言；针对目标语言适当地评估其语法和语义\n"
            "- 依赖上下文：利用周围的代码上下文来评估语义的准确性\n"
            "- 位置无关：独立评估每个预测结果；避免位置顺序影响你的判断\n\n"
            f"{PromptLibrary.expert_accuracy_guidance()}\n\n"
            "返回每个模型结果的结构化 JSON：\n"
            "{\n"
            '  "评估结果": [\n'
            "    {\n"
            '      "模型名称": "<模型标识符>",\n'
            '      "推理": "逐步分析：首先，我观察到...",\n'
            '      "错误": [\n'
            "        {\n"
            '          "错误标识": "<错误标识>",\n'
            '          "置信度": <0.0-1.0>,\n'
            '          "理由": "此错误适用，因为..."\n'
            "        }\n"
            "      ],\n"
            '      "整体质量": "<正确|部分正确|不正确>"\n'
            "    }\n"
            "  ]\n"
            "}\n"
            "如果模型没有错误，请返回空的错误数组，并在推理中解释为什么该注释是正确的。"
        )
